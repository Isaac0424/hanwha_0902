pip install "fastapi[standard]"
(fastapi, uvicorn 까지 설치)
실행 방법  
fastapi dev main.py             # 단일 파일 추천
uvicorn main:app --reload       # 엔트리 포인트를 메인으로 시작

---------------------------
주요 옵션 설명
main: 파일명 (main.py에서 .py 제외)
app: 코드 내부의 FastAPI 인스턴스 변수명 (app = FastAPI())
--reload: 코드가 수정될 때마다 서버를 자동으로 재시작 (개발용)
--host 0.0.0.0: 외부 접속 허용
--port 8000: 포트 번호 변경 (기본값: 8000)

---------------------------
localhost:8000/docs 
swagger ui가 현재 api 들을 자동으로 샘플을 뽑아준다. 
테스트하기 편하다.

---------------------------
fastapi 는 pydantic설정을 내부적으로 하고 있어서 관리받고 있다는 느낌을 받을 수 있다.

---------------------------
form 없이 요청을 숨긴체로 보내고 싶으면 post요청을 해야한다.


===========================
API를 작성할 때는 API 문서를 먼저 작성해야 한다.
API 문서 설계 및 구성 가지고 개발에 들어가야 혼선이 없다.
===========================

Query라는 것은 질문이라는 의미로 url에서 경로를 제외하고 '?'로 시작하는 값들이다 Key-Value로 쌍을 이루며 '&'으로 구분된다.

# HTTP Method 비교표

| Method | 주 용도 | 서버 데이터 변경 | Idempotent | Body | FastAPI 중요도 |
|---|---|:---:|:---:|:---:|:---:|
| `GET` | 조회 | ❌ | ✅ | 보통 ❌ | ★★★★★ |
| `POST` | 생성 / 작업 실행 | ✅ | ❌ | ✅ | ★★★★★ |
| `PUT` | 전체 수정 / 교체 | ✅ | ✅ | ✅ | ★★★★☆ |
| `PATCH` | 일부 수정 | ✅ | 설계에 따라 다름 | ✅ | ★★★★★ |
| `DELETE` | 삭제 | ✅ | ✅ | 보통 ❌ | ★★★★☆ |
| `HEAD` | GET의 헤더만 조회 | ❌ | ✅ | ❌ | ★★☆☆☆ |
| `OPTIONS` | 지원 메서드 / CORS 확인 | ❌ | ✅ | 보통 ❌ | ★★★☆☆ |
| `TRACE` | 요청 경로 진단 | ❌ | ✅ | ❌ | ★☆☆☆☆ |

## 핵심 정리

- **GET**: 데이터를 조회할 때 사용
- **POST**: 새로운 리소스를 생성하거나 작업을 실행할 때 사용
- **PUT**: 리소스 전체를 교체하거나 수정할 때 사용
- **PATCH**: 리소스의 일부 필드만 수정할 때 사용
- **DELETE**: 리소스를 삭제할 때 사용
- **HEAD**: GET과 유사하지만 Response Body 없이 헤더만 확인
- **OPTIONS**: 서버가 지원하는 HTTP Method 확인, CORS Preflight에서 자주 사용
- **TRACE**: 요청 경로 진단용. 일반적인 REST API에서는 거의 사용하지 않음

## Idempotent란?

같은 요청을 여러 번 보내더라도 **최종 서버 상태가 동일한 성질**을 의미합니다.

예:

```http
PUT /users/10

{
  "name": "Daeyoung"
}
```

위 요청을 여러 번 보내더라도 최종적으로 `name = Daeyoung`이라는 서버 상태는 같습니다.

반면:

```http
POST /orders
```

를 여러 번 보내면 주문이 여러 개 생성될 수 있으므로 일반적으로 Idempotent하지 않습니다.
